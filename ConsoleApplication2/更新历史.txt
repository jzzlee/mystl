更新说明
	
	v1.5.1
	完成priority_queue.

	v1.4.2
	完成heap.

	v1.4.1
	完成heap.

	v1.3.1
	使用vector实现stack,使用list实现queue.

	v1.2.6
	未完成insert.

	v1.2.5
	修改完assign.

	v1.2.4
	修正map中控器的结构，使其支持快速双端操作。
	修改到assign部分。
		
	v1.2.3
	实现operator=, assign, get_allocator, at, [], front, back, empty, size, max_size, shrink_to_fit, clear.
	由于预置的数据结构不合适，删除deque中的reverse_iterator操作。

	v1.2.1
	实现deque的iterator, reverse_iterator，constructor, destructor, begin, end.

	v1.1.7
	实现完整的list.

	v1.1.6
	实现list的splice, remove, remove_if.

	v1.1.5
	实现list的push_back, pop_back, push_front, pop_front, emplace_front, resize, swap, merge.

	v1.1.4
	实现list的assign, clear, insert, emplace, emplace_back,	erase.

	v1.1.3
	实现list的operator=
	
	v1.1.2
	实现list的reverse_iterator

	v1.1.1
	实现list的constructor， destructor, iterator, reverse_iterator, front, back, begin, cbegin, end, cend, empty, size, max_size.

	v1.0.11
	实现完整的vector容器（不包括反向迭代器）。

	v1.0.9
	实现vector的erase， push_back函数

	v1.0.8
	实现了vector的clear, insert函数

	v1.0.7
	实现了vector的assign, get_allocator, at, operator[], front, back, data, begin, cbegin, end, cend, empty, size, max_size, capacity, reverse, shrink_to_fit函数

	v1.0.6
	实现了vector的operator=

	v1.0.5
	实现了vector的构造函数，析构函数

	v1.0.4
	实现type_traits,修正了uninitialized_copy, uninitialized_copy_n, uninitialized_fill and uninitialized_fill_n.

	v1.0.3
	实现iterator, iterator_traits, advance, distance

	v1.0.2
	实现4个内存基本处理工具uninitialized_copy, uninitialized_copy_n, uninitialized_fill and uninitialized_fill_n.

	v1.0.1
	实现allocator类的基本操作